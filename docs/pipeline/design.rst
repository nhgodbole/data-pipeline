Pipeline Design
===============

This data flow is orchestrated using the ??? framework, which handles and monitors the pipeline as a whole.
This is bundled into a CODEX specific processing tool, accessible here ???
Individual flows are defined for each data level, which outline the series of pipeline modules that the data pass through.

The code snippet below provides an example for a CODEX L1 flow.